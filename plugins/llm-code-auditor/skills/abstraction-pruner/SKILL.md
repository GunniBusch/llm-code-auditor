---
name: abstraction-pruner
description: Use when code has one-off interfaces, abstract classes, factories, managers, services, processors, strategies, plugins, event buses, wrappers, adapters, or delegation layers that may be over-engineered, speculative, pass-through, or generated by an LLM.
---

# Abstraction Pruner

Use this skill to remove abstractions that do not earn their complexity. This is not an anti-abstraction skill: keep boundaries that make the code safer, clearer, more testable, or more adaptable to real current requirements.

## Workflow

1. Map each layer in the call graph and write what invariant it adds: mapping, validation, authorization, transaction, caching, retries, lifecycle, observability, or polymorphism.
2. Search for all implementations and callers before editing.
3. Keep boundaries that protect external services, public APIs, tests for hard dependencies, security, concurrency, persistence, or serialization.
4. Search for an existing repo concept that already expresses the same domain idea before inventing a new replacement.
5. Collapse layers that only forward unchanged arguments, hold one implementation, or exist for "future flexibility."
6. Rename after collapse using concrete domain nouns.
7. Run tests and type checks that cover the call path.

## Refactor Rules

- Inline helpers called once unless the extracted name is a real domain concept.
- Replace one-implementation interfaces with concrete types unless the boundary is real today.
- Replace factories with direct construction unless object creation is conditional or complex.
- Replace strategy/plugin/event infrastructure with direct calls until a second real participant exists.
- Prefer two clear domain branches over a generic framework for imagined future cases.
- Keep a single implementation when it is a real boundary: protocol contract, security/auth, transaction, lifecycle, external service, or domain vocabulary used across the repo.

Read `../llm-code-auditor/references/senior-refactor-playbook.md` for abstraction judgment and `../llm-code-auditor/references/pattern-catalog.md` for single-use abstraction, pass-through layers, speculative extensibility, and AI symmetry examples.
